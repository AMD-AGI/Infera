# yaml-language-server: $schema=../_schema/patch.upstream.status.schema.json
schema_version: 1
status_updated: 2026-08-24
verified_by:
  - gh-pr-issue-state
  - gh-search
  - upstream-source-read
  - local-repro

patch:
  path: deploy/docker/patches/sglang_rocm/patch_hicache_rocm_host_alloc.py
  kind: python-anchor-script
  idempotent: true
  applied_by:
    - Dockerfile.sglang
    - Dockerfile.sglang.gfx942
  opt_in_flag: null

target:
  library: sglang
  component: hicache
  repo: sgl-project/sglang
  files:
    - python/sglang/srt/mem_cache/pool_host/common.py
  versions:
    - surface: Dockerfile.sglang
      ref: v0.5.15.post1
      commit: 0b3bb0cbe318
      pinned_ref_on_main: false
      release_branch: release/v0.5.15
      images:
        - tag: lmsysorg/sglang:v0.5.15.post1-rocm720-mi35x
          digest: null
          digest_source: unresolved
          note: gfx950 — where the fault was actually observed.
    - surface: Dockerfile.sglang.gfx942
      ref: v0.5.16
      commit: fdebc938f7f4
      pinned_ref_on_main: false
      release_branch: release/v0.5.16
      images:
        - tag: lmsysorg/sglang:v0.5.16-rocm720-mi30x
          digest: null
          digest_source: unresolved
          note: >-
            Carried preventively here. MI300X measures the host and device addresses
            EQUAL, so this image has no fault to fix.

upstream_main_affected:
  value: true
  evidence: >-
    Read from upstream main on 2026-08-05 (contents API, pool_host/common.py):
    ALLOC_MEMORY_FUNCS still overrides only "npu" and "musa", with no HIP entry, over a
    defaultdict whose default is alloc_with_host_register. Main is affected. This is a
    source read, not a search miss.

applies_to:
  - engine: sglang
    surface: Dockerfile.sglang (gfx950 / MI355X, v0.5.15.post1)
    effect: op
    required_to_run: true
    evidence: >-
      Without it the first kvd write-back aborts the process with a GPU memory access
      fault. Reproduced standalone with the exact write-back kernel.
  - engine: sglang
    surface: Dockerfile.sglang.gfx942 (gfx942 / MI325X, v0.5.16)
    effect: op
    required_to_run: false
    evidence: >-
      It changes the allocator here too, but preventively: MI300X (amdgpu 6.14.14, ROCm
      7.2.0) measures hipHostRegister's device pointer EQUAL to the host VA, so the
      fault does not occur on this arch. Do not read this row as evidence the fault was
      seen on gfx942.

alive_because:
  reason: upstream-pr-open
  detail: >-
    Ours is now upstream as sglang#33968 (DRAFT, opened 2026-08-07). No third-party
    issue or PR exists. Upstream main is affected, established by reading the file, so
    this is not a case of "probably already fixed". Was `no-upstream-pr` until #33968.
  consumers:
    - Dockerfile.sglang (kvd / hierarchical cache on gfx950 — required)
    - Dockerfile.sglang.gfx942 (preventive)
  drop_when: >-
    A base sglang routes HIP to alloc_with_pin_memory in ALLOC_MEMORY_FUNCS.
  drop_signal: anchor-drift-fails-loudly
  silent_misapply_risk: null

history:
  born:
    date: 2026-08-03
    commit: 9380f07
    subject: "fix(glm5.2): agentic-serving fixes for DSA indexer, ROCm hicache and PD/kvd wiring"
  last_modified:
    date: 2026-08-04
    commit: 71fe42c
    subject: "fix(sglang-rocm): make hicache survive its first write-back on gfx942"
    reason: >-
      Carried onto the gfx942 image alongside the staged write-back patch, and its
      header corrected: the fault is gfx950-only so far, so this one is preventive on
      MI300X rather than a fix for an observed crash.

upstream_issues: null

upstream_prs:
  - ref: sgl-project/sglang#33968
    url: https://github.com/sgl-project/sglang/pull/33968
    title: "[ROCm] Fix HiCache host-pool allocator: hipHostRegister's device pointer is not the host VA"
    state: OPEN
    review_decision: REVIEW_REQUIRED
    merged_at: null
    author: dorado269
    ours: true
    same_approach: true
    approach_note: >-
      Opened 2026-08-07 as a DRAFT. Same fix as this patch, minus the local
      GLM52_ROCM_HOST_ALLOC bytecode marker. Adapting it corrected the record's own
      framing twice: get_device() returns "cuda" on ROCm, so HIP cannot take a key of
      its own the way #23361's "musa" did; and memory_pool_host.py:768 and :1257 key
      the table with a torch.device OBJECT, which is not dict-key-equal to the string
      "cuda" (measured: different hash), so those two pools always resolve through the
      defaultdict default. Changing only the key would have missed them — which is why
      both the key and the default move.
    requested_action: >-
      Awaiting review — no longer draft, no longer blocked. Taken out of draft
      2026-08-24 after the gfx950 validation the previous record was waiting on. Head is
      138901d918, rebased onto upstream main 56834422a1 (clean, one file). A controlled
      A/B on crsuse2-m2m-237 (MI355X) reproduced the fault on the stock allocator and
      showed it gone on the patch; the evidence is posted as a PR comment, so the
      write-back repro in the PR body is no longer the only, historical, support for it.
      One consequence was disclosed there rather than left for a reviewer: on ROCm the
      patch routes to alloc_with_pin_memory, which ignores the `allocator` argument, so
      a configured mooncake/mori storage backend is bypassed — same as "npu"/"musa"
      already do, and strictly better than the fault, with the narrower
      allocator_type == "default" form offered if maintainers prefer it.
    in_pinned_base: false

related_refs:
  - ref: sgl-project/sglang#23361
    url: https://github.com/sgl-project/sglang/pull/23361
    kind: pr
    title: "[MUSA][19/N] Support HiCache with pin_memory allocator"
    state: MERGED
    state_reason: fixed
    author: yafengio
    relevance: shape-we-copied
    note: >-
      The same one-line dispatch override for the same reason, for MUSA. This patch is
      written in its shape, which is also the argument that upstream would accept a HIP
      entry.
  - ref: sgl-project/sglang#32503
    url: https://github.com/sgl-project/sglang/pull/32503
    kind: pr
    title: "[XPU] Enable HiCache support on Intel XPU"
    state: OPEN
    state_reason: null
    author: kpjeeja
    relevance: anchor-collision
    note: Touches the same ALLOC_MEMORY_FUNCS dict. Expect a conflict, not a fix.
  - ref: sgl-project/sglang#32792
    url: https://github.com/sgl-project/sglang/pull/32792
    kind: pr
    title: "[XPU]Enable HiSparse hierarchical sparse KV cache on Intel XPU"
    state: OPEN
    state_reason: null
    author: Amrutha-M05
    relevance: anchor-collision
    note: Same dict again.

problem:
  what: >-
    sglang allocates every hierarchical-cache host pool with anonymous mmap +
    hipHostRegister, then stores the resulting HOST virtual addresses in a device-side
    pointer table that a GPU kernel dereferences. On ROCm those host VAs are not
    dereferenceable from the device.
  why: >-
    On CUDA, cudaHostRegister maps the pages at the SAME address in the device address
    space, so a host VA is directly usable from a kernel. On ROCm, hipHostRegister maps
    them at a DIFFERENT device address which you must obtain via
    hipHostGetDevicePointer. Measured on gfx950 / ROCm 7.2.0 with an 8 MiB buffer:
    pin_memory gives host==devPtr, while mmap+hipHostRegister gives
    host=0x7d3bee790000 devPtr=0x7d3bede00000 — and every register-flag variant
    (Mapped, Portable|Mapped, MAP_PRIVATE) also differs. gfx950 reports xnack-, so
    there is no page-migration path to paper over it.
  how: >-
    Register ROCm in ALLOC_MEMORY_FUNCS to use alloc_with_pin_memory — torch
    pin_memory=True, hipHostMalloc underneath — which returns memory whose device
    pointer IS the host pointer. Exactly what the existing "npu" and "musa" entries do,
    for the same reason. This is the smallest change that makes the existing
    pointer-table design correct on ROCm; translating every host pointer through
    hipHostGetDevicePointer is the right upstream shape but touches roughly ten call
    sites and is not something to apply blind to a running container.
  before_fix: >-
    "Memory access fault by GPU node-2 (Agent handle: ...) on address <host VA>. Reason:
    Unknown." on the first kvd write-back. The fault address equals the host pointer
    exactly, which is the fingerprint.
  after_fix: >-
    Standalone repro of the exact write-back kernel — 78 layers, 7.33 GB host indexer
    buffer, page ranges head / tail / last — goes from a fault on every mmap variant to
    ALL OK with pin_memory.
  context: >-
    gfx950 / MI355X, ROCm 7.2.0, sglang hierarchical cache with a DSA model
    (DSAIndexerPoolHost). MI300X measures the two addresses equal, so the fault is
    gfx950-only so far. Cost of the fix: the hugepage path in alloc_mmap
    (SGLANG_HUGEPAGE_SIZE) is bypassed on ROCm; that env var is unset here.
  call_chain:
    - "sglang/srt/mem_cache/pool_host/common.py :: ALLOC_MEMORY_FUNCS[device] -> alloc_with_host_register"
    - "alloc_mmap(dims, dtype) then cudaHostRegister(buffer.data_ptr(), n, 0)"
    - "memory_pool_host.py :: DSAIndexerPoolHost.init_kv_buffer builds index_k_data_ptrs from host data_ptr()s on the GPU"
    - "transfer_kv_all_layer_mla(dst_layers=self.index_k_data_ptrs, ...) — kernel dereferences a host VA"
  symptom_signature: "Memory access fault by GPU node-N ... on address <host VA>"
  silent: false

verification:
  date: 2026-08-03
  hardware: MI355X (gfx950, xnack-) and MI300X (amdgpu 6.14.14) for the negative control
  software: ROCm 7.2.0, sglang v0.5.15.post1
  workload: standalone repro of the hicache write-back kernel, 78 layers, 7.33 GB host indexer buffer
  result: mmap+hipHostRegister faults on every variant; pin_memory is clean on all page ranges
  notes: >-
    The address-equality measurement is the cheap way to tell whether a given arch is
    affected — run it before assuming a new GPU needs this. But it is arch-INDEPENDENT
    only in the sense that it must be re-run per machine: two gfx950 boxes disagreed.
    n06-33 (host ROCm 7.2.0) measured host==devPtr for every strategy at every size from
    8 MiB to 7.33 GB and could NOT reproduce the fault; crsuse2-m2m-237 (host ROCm 7.0.1)
    reproduces it. Both run amdgpu 6.14.14, so the driver version — previously the
    leading hypothesis — is ruled out. No mechanism is claimed; host ROCm version and
    container torch/hip build both differ and were not separated.

    Re-validated 2026-08-24 on crsuse2-m2m-237 (MI355X gfx950 xnack-, amdgpu 6.14.14,
    host ROCm 7.0.1; container torch 2.9.1+rocm7.2.0, hip 7.2.26015, sglang v0.5.17
    base) against sglang#33968 head 138901d918. Four links, all first-hand:
    (1) pointer probe — pin_memory same=True, all three hipHostRegister variants
    same=False, deterministic over three runs; (2) dispatch resolved against the real
    table — stock sends "cuda", device("cuda:0") and device("cuda") all to
    alloc_with_host_register, patched sends all three to alloc_with_pin_memory;
    (3) transfer_kv_all_layer_mla driven with dst_layers built as pool_host/dsa.py:159
    builds it — stock faults with "Memory access fault by GPU node-2 on address
    0x56a0af6b9000", patched completes and the copied pages carry the expected pattern,
    both reproduced twice; (4) the fault address is of the host-VA form (host buffers at
    0x5aa6..., device mappings at 0x74b0...). validate_A.py PASS on the same machine.
    This is a targeted harness driving the real kernel, the real pointer-table
    construction and the real allocator dispatch — not a full model bring-up: sufficient
    for the causal claim, but it does not exercise the HiCache bookkeeping around it.
    Artifacts: pr-verify.experiment/rounds/r03-newcluster/.

scope_limits: >-
  Fixes the allocator, not the design. The pointer tables still hold raw host pointers;
  any future pool that builds a device-side table the same way is correct only because
  pin_memory happens to return matching addresses.

open_actions:
  - action: >-
      Watch sglang#33968 for review. Validation is done and the PR is out of draft; the
      remaining work is upstream response. One point was disclosed in the PR comment
      rather than left for a reviewer to find: on ROCm the patch routes to
      alloc_with_pin_memory, which ignores the `allocator` argument, so a configured
      mooncake/mori storage backend is bypassed. That mirrors what "npu" and "musa"
      already do and is strictly better than the fault, but if maintainers prefer the
      narrower form (route only when allocator_type == "default"), that is offered.
    owner: dorado269
    blocked_on: upstream review
