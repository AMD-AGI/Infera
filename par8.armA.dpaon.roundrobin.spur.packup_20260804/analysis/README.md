# Analysis

| file | what |
|---|---|
| `routing_distribution.md` | **the arm's headline** — two independent measurements that round-robin spreads across all 8 DP ranks, the kv-aware contrast, and what it settles |
| `sli_percentiles.md` | every latency / throughput / cache number, whole-run and per-phase, from `results/summary.json` |
| `feature_evidence.md` | the five features one by one, each with its signal |
| `two_arm_comparison.md` | **arm A vs arm B side by side** — and why the pair supports no single-variable attribution |

Start with `routing_distribution.md`. The latency numbers are context; the routing
distribution is the result.
