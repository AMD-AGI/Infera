## 2026-09-10T04:58:21Z — Setup checkpoint

- **Progress / elapsed / ETA / confidence:** Approximately 10% overall (subjective setup estimate); 0/2 benchmark points completed. Elapsed 6m10s since `state/START_TS` (2026-09-10T04:52:10.704716+00:00). Completion ETA is not yet estimable; low confidence until image loading, resource release, and server initialization finish.
- **Current state:** Workspace instructions, reference launch/benchmark files, three fake-only patches and corresponding source files, and adapted scripts are present. The requested experiment is fake decode TP4/EP4, ISL10000/OSL500, concurrency16 and32, with 16 warmup plus128 measured requests per point. Preserve the previous fake/simulated-accept3.61 settings except the requested parallelism, lengths, and concurrency changes. Prepared launch configuration includes EAGLE5steps/6draft/topk1 and match-expected/real-draft-token simulation; runtime behavior has not been checked by this scribe. Main reports image loading and user-authorized stopping of known GPU training containers in progress on node249/job130740 only; no experiment server yet. Local `image-load.log` and `stop-authorized-workloads.log` remain empty, and no round/result artifacts were present at this checkpoint, so completion of those operations is unconfirmed.
- **Code issues:** No runtime code failure is documented yet; launch and measurement have not produced logs. Prepared launcher uses an initial memory fraction of0.85, as specified in the workspace plan; whether this preserves the user's requested settings parity remains unverified. No code changes or scientific verification were performed by the scribe.
- **Non-code issues:** The saved pre-stop GPU snapshot reports90–91% VRAM allocation across all eight GPUs. Container-command and ownership snapshots are available, but these historical snapshots do not establish current availability. Image-load completion and GPU release remain prerequisites. Allocation expiry is2026-09-10T11:52:45Z. Peer node056/job130737 is out of scope and must remain untouched.
- **Unknowns / next evidence:** Image-load and authorized-stop outcomes; idle-gate result; server readiness/JIT duration; actual token lengths, request success counts, batch occupancy, acceptance length, graph/backend dispatch, and throughput for each point. These are main's pending checks, not findings. No benchmark or performance conclusion is supported yet.
- **Commits:** None reported or requested; no commit/push performed by the scribe. This entry is the only scribe write. The previous packup task remains complete and is not reopened; future timer resumptions will append one entry per30minutes without a resident sleep loop.

## 2026-09-10T05:24:18Z — C16 complete; C32 starting on replacement node

- **Progress / elapsed / ETA / confidence:** Approximately60% overall (subjective workflow estimate); 1/2 measured points complete. Elapsed32m07s since `state/START_TS`. Provisional remaining time15–30minutes (approximately05:39–05:54Z), low confidence: C32 startup/graph capture, measurement, reporting, and cleanup remain. C16 recorded summary metrics have high confidence as benchmark-file facts, not semantic correctness findings.
- **Current state:** Active resource is node036/job130891, allocated until2026-09-10T11:01:03Z, after peer-coordinated replacement excluding056 and249. This supersedes the earlier249-only setup status and the stale resource paragraph in `CLAUDE.md`; the scribe has not changed that file. `state/leader.md` records successful authorized workload stop and idle gate on036, then C16 launch and readiness. Main reports C16 graceful stop was issued. Fresh C32 launch artifacts now exist: `rounds/c32/start-time.txt` is05:22:14Z; its latest read readiness entry is05:24:02Z, state`starting`, curl exit7. Thus C32 has advanced beyond merely being next, but no C32 measurement result is present. Its saved command uses TP4/EP4 and max-running/graph-max32; main reports otherwise unchanged configuration and simulation settings. Each point retains16 warmups and128 measured requests, ISL10000/OSL500.
- **C16 recorded results:** `rounds/c16/benchmark.jsonl` reports128 completed requests, duration35.49353097s, 64,000 output tokens, output throughput1803.14548196tokens/s for the four-GPU instance, P50 TPOT8.77540912ms, P90 TPOT8.91123659ms, and acceptance length3.59733481. Main additionally reports128/128 successful requests, all per-request lengths10000/500, and114 scheduler log entries with running16 and zero retracted requests; those per-request/log checks were not repeated by the scribe. These are synthetic-KV/simulated-accept benchmark results, not semantic correctness or production performance evidence.
- **Code issues:** No new fatal code issue is established. Leader notes EP4 MoE heuristic FlyDSL fallback where tuned configurations are absent, and multimem all-gather disabled because Torch lacks `set_signal_pad_size`; neither alone establishes a correctness failure or performance regression. C32 verification may reach192 rows and exceed the FlyDSL96-row gate, so actual fallback/dispatch remains pending rather than a confirmed outcome. C32's starting/curl7 readiness entries alone do not establish failure.
- **Non-code issues:** Node249 was closed as blocked for this experiment: leader records all nine authorized graceful stops failed after300s with no exit event, alongside D-state/`flush_workqueue` observations. No reset/reboot/daemon changes or repeated kill attempts were pursued; the original user-provided hold was not cancelled. Node056/job130737 remains peer-owned and untouched. Old image-loading/resource-release uncertainty on249 must not be carried forward as the current036 experiment state.
- **Unknowns / next evidence:** C32 readiness, any first worker error, measured request/length/occupancy/acceptance evidence, actual graph/backend dispatch, C16-versus-C32 comparison, and final own-container/GPU-release confirmation. No C32 metric or semantic conclusion is available. The scribe only read local artifacts and coordinator status; no GPU commands or scientific verification were run.
- **Commits:** None reported or requested; no commit/push. This invocation appends exactly one checkpoint section and returns without sleeping. Previous packup work stays closed.

## 2026-09-10T05:36:09Z — Final checkpoint: both points complete and resources released

- **Progress / elapsed / ETA / confidence:** 100% of the requested two-point experiment and cleanup complete; 2/2 points measured. Elapsed43m59s since `state/START_TS`. Remaining ETA0. High confidence for the recorded summaries and cleanup evidence read from `results/summary.json`, `results/REPORT.md`, `logs/final-cleanup.txt`, and the latest COMPLETE entry in `state/leader.md`; this is artifact transcription, not a new scientific verification.
- **Final results:** C16:128/128 successful,35.493531s,1803.145482 output tokens/s per four-GPU instance (450.786370/GPU), P50/P90 TPOT8.775409/8.911237ms, acceptance3.597335. C32:128/128 successful,24.911468s,2569.097920 output tokens/s per four-GPU instance (642.274480/GPU), P50/P90 TPOT11.632978/13.871815ms, acceptance3.618010. The final report records zero errors and exact10000-input/500-output lengths for every measured request in both points, each preceded by16 warmups. Sampled scheduler counts are114 C16 entries running16; C32 has56 entries running32 and one running16. All sampled retracted queues are zero; these are sampled counts, not time-weighted occupancy.
- **Current state / cleanup:** Both points ran on node036/job130891, GPUs0–3, after the coordinated replacement excluding249 and056. The05:30:59Z cleanup artifact records container exit0 and all eight GPUs at0% VRAM allocation, with only collector/exporter containers remaining. Its KFD listing includes PID88564 with zero GPU count/VRAM/occupancy, so no claim of an empty KFD listing is made. Latest leader state records cancellation of the experiment-owned replacement allocation130891 after cleanup; user-provided holds were left untouched. Main reports the leader timer was removed. No further measurement or checkpoint timer work is requested.
- **Code issues / backend qualification:** C32 graph capture explicitly declined FlyDSL sparse MLA at verification shape192 because the kernel accepts1–96 rows; fallback remained enabled. Lower-row/draft shapes can still use FlyDSL, so C32 is neither proven entirely FlyDSL nor entirely fallback. EP4 MoE heuristic fallback and the Torch `set_signal_pad_size`/multimem limitation remain recorded implementation choices, not established correctness failures. No engine changes beyond the copied three proven fake-only patches are reported.
- **Non-code issues / resource boundary:** Node249 remains a closed blocked attempt after D-state/`flush_workqueue` observations and failed authorized graceful stops; no reset or recovery claim is made. Peer node056 was never accessed. The final cleanup lists the preserved exporter as unhealthy; it was not modified or investigated by the scribe.
- **Interpretation / remaining unknowns:** Synthetic KV plus simulated acceptance3.61 does not test semantic correctness or production PD performance. The report's observed C32-versus-C16 throughput improvement42.48% comes with higher P50 TPOT32.56% and P90 TPOT55.67%; these are single short-run observations, not repeat-run significance, stability, or topology-only speedup evidence. Exact per-call backend performance attribution was not profiled. No requested experiment step remains pending; these scientific limitations remain explicit rather than being treated as validated conclusions.
- **Commits / closeout:** No new commits or pushes; no user repository changes reported. This invocation appends one final checkpoint section only, performs no GPU commands or scientific verification, and returns without sleeping. Previous packup work remains closed.
