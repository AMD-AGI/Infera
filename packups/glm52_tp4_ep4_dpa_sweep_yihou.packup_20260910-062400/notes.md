# Interpretation and operational notes

- **Counts:** globalC is localC off,localC/4 on; four TP copies off count once, four DP shards on count once each. Complete=true means exact useful output count,not output-text correctness. Terminal extra2tokens/request are computed but excluded from useful count.
- **Timing:** actual draft+verify+draft-extension,CPU bookkeeping,synchronization and7-int cross-rank progress all-gather are included. Startup/capture/bootstrap/warmup are excluded. The extra validation collective did not exist in the earlier TP8 result,so cross-experiment deltas are not pure topology comparisons.
- **Fallback:** allDPAon cases decline FlyDSL sparse MLA64head shape; offC20/C24 verify exceeds96rows. Graph counters still pass because graph capture includes fallback kernels. Decode-engaged markers from draft do not establish the verify path used FlyDSL. Some MoE shapes use heuristic configs. Preserve these defaults for reproduction; widening kernel support would be a separate experiment.
- **Cold startup:** first smoke891s versus hot fullpoints153–217s. Post-shard quiet period resolved without restart and progressed to norm/rope JIT. No hang fix was demonstrated. Container cache persists but each invocation reloads weights.
- **Reference data:** real weights and routing, synthetic broad positive finite FP8 prefix/index initialization,forced shared acceptance coins. No natural-prompt KV statistics,independent per-request acceptance,eager/graph numerical equivalence,quality eval or client ITL distribution was validated.
- **Shared node:** user explicitly authorized stopping two identified non-party GPU containers after ownership/PID inspection on056; collectors preserved. This history is not permission for future reproducers to kill workloads. Another observed idle container was left untouched. No continuous exclusion monitoring; report shared-cluster limitation.
- **Storage/device namespaces:** Spur /dev/dri and root space differ from Docker host view. Probe actual selected-device and containerd storage. Never infer driver failure from empty namespace devices or prune others' images.
- **Git traceability:** original launcher records tracked diff plus full bench snapshot and hashes; new untracked topology.py is preserved by snapshot. No commit or push was performed. The frozen bench bytes match all12 measured cases.
- **Archived text:** original working_process still contains in-progress entries; later report/results are final. Original README has oldTP8/example paths and is retained under history,not normative. Original per-run command paths point to the measured checkout; use new REPRODUCE.md to relocate. Original logs are now .log.gz without trimming. Python __pycache__ excluded,not deleted from sources.
- **No new GPU work:** packaging validates files/tests/metrics only. Existing image and weights remain external. Missing056CPU/RAM/RDMA and full weight identity are explicit gaps; no guessed snapshots.

No original files moved/deleted/overwritten;no containers or allocations touched for packaging;no secret values intentionally included. Signature scans only detect known formats,not arbitrary secrets. No statistical confidence or general DPA performance claim from this n=1 matrix.
