# Notes

| file | what |
|---|---|
| **`notes.lat1.md`** | **the one to read.** Why each of the seven changed knobs is load-bearing (with the source line that makes it so), the two defects found and fixed, the sample-size limit, and an explicit list of what this run does *not* establish |
| `notes.config.CASEA.md` | carried from the Case A kit. Documents the `--mem-fraction-static` 0.88 → 0.80 fix (still in force here) and the three wrong root causes published before it was found |
