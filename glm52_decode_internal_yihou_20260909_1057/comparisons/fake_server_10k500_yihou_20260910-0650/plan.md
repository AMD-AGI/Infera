# Validate internal decode benchmark against server reference

## Context
Compare our scheduler-free internal method against `/shared_nfs/yihou/packups/glm52_fake_tp4ep4_10k500_c16_c32.packup_20260910-055810`, primarily validating our wrapper. The reference has exactly two measured configurations: TP4/EP4/DP1, DPAoff, ISL10000/OSL500, concurrency16/32,16 warmup requests then128 measured requests each. Same imageb9a83742f631, SGLang402df1e1 and AITER2c71811b,FP8KV,EAGLE5/6/topk1,simulated accept3.61 match-expected/real-draft-token,memfraction0.85.

Reference is an actual decode-server execution with fake handoff, not real P→D transfer or semantic correctness. The server uses overlap scheduling and128 streamed requests; our baseline uses a synchronous fixed batch and full-loop timing. Reference P50TPOT is `(latency-TTFT)/(output_len-1)`, whereas our prior metric is elapsed/output_len. Close throughput alone cannot prove either method correct.

## Scope and resource
Code only in `/home/yihou/dev/git/infera.dev.yihou.sglang.bench.fast.script/glm52_decode_internal_yihou_20260909_1057`. Preserve existing dirty changes,baseline4d982fa,and all prior results/packups. No commits/pushes unless requested.

User explicitly approved **271/job131080 and cleanup** for this task; peer confirmed no work on271 and uses249. Inspect precise workload identity before stopping authorized non-party GPU tasks; preserve own-party work and system collectors. No249/234/036;no new allocation requests/cancellations. Load pinned image on271 after checking actual storage/GPU baseline. New owned container, four GPUs,read-only model/source reference mounts,write only current task workspace. Do not replay historical reference cleanup commands.

Back up current benchmark CLAUDE.md with `CLAUDE.tp4-ep4-dpa-sweep.<timestamp>.md.bak`; write current task instructions. Create `comparisons/fake_server_10k500_yihou_<timestamp>/` with reference extraction, frozen source snapshots, per-iteration logs and report. Old packups remain read-only.

## Recommended comparison sequence
1. **Extract and audit reference.** Validate manifest and recompute saved client metrics with reference code/formulas. Record exact server command,resolved flattened server-info,container performance env,patched-file identities,request counts/lengths,graph sizes/occupancy,overlap behavior and initialization. Reference server seeds are197604707(C16),1017015168(C32);client sampling seed and consumed RNG history are separate. Preserve reference per-request P50/P90 and ITL quantiles without relabeling them.
2. **Run unchanged internal baseline at both matched shapes.** TP4EP4DP1,C16/32,10000/500,mem0.85,same optimizations and actual model. Explicit max-running requests matchesC. Use exact reference configuration where applicable; retain and label synchronous/non-PD structural differences. Run multiple fixed-batch repetitions after warmup so total measured request count is128 (8waves atC16,4atC32), without turning this into a scheduler/server. A single-wave initial smoke may precede this. Do not treat16 warmup requests as16 decode iterations.
3. **Align synthetic state where evidence supports it.** Reference fake handoff supplies dummy bonus/proposal/hidden state and has three fake-only patches (proposal one-hot,index-K elision,physical top-k domain). Inspect the actual initializer (fetch exact pinned source if absent from bounded packup), then add an opt-in reference-compatible initialization mode if necessary. Reuse existing appropriate allocation/index conventions; do not blindly apply PD-only patches to mix-mode internal workers or disable optimized kernels merely to match numbers. Keep current SIKL-style initialization as a named baseline. Implement only small wrapper changes; ask before large core patches.
4. **Validate execution independently of timing.** Check actual physical KV/index allocation,request lengths,bonus/accept semantics,recurrent draft state,local/global counts,terminal clipping and target/draft/extension graph calls. Compare stage shapes and dispatch/fallback with reference. C32verify192rows must log the same96-row FlyDSL fallback. For identical internal initial state, validate eager versus graph or a direct pinned-worker reference on a cheap bounded segment where practical; log output/state differences and tolerances explicitly rather than asserting text correctness under simulated acceptance.
5. **Separate timing boundaries and diagnose differences.** Record full internal loop elapsed,mean/per-wave TPOT,per-stage timings if needed,bootstrap/warmup/startup. Compare aggregate output throughput and distributions only with definitions visible side-by-side. Server128request wall time includes refill/tail/streaming; synchronous waves omit/alter them. Quantify obvious normalization effects and report remaining scheduling/overlap/hardware/random-input differences. Change one proven cause per rerun,retain baseline. No goal of forcing numerical agreement; no arbitrary percentage constitutes correctness.
6. **Report.** Two configuration rows plus baseline/aligned variants if tested,absolute/relative differences,reference vs internal metric definitions,source/image/model provenance,rank/stage/token validation,actual fixes and unresolved uncertainties. Mark evidence-backed 'validated within scope' separately from 'performance close' and from true-PD/semantic correctness (not tested). If substantial mismatch remains without evidence,leave it open rather than declare success.

## Code changes (only as needed)
- `bench/profile_decode.py`: optional repeated fixed-batch waves and explicit reference-compatible initialization/timing modes; preserve existing CLI defaults/results.
- `bench/batch_state.py` / focused new helper: request/wave and first-token/terminal accounting.
- `tests/`: CPU coverage for128request accounting,16warmup-request semantics,OSL500 terminal cap and metric normalization;existing23tests remain passing.
- Comparison-specific launch/collector scripts under new comparison workspace or scripts/: exact parameter extraction, guarded resource launch, paired metric report.
- Required runtime/speculative/env skills apply before edits. Use LSP/Serena/source inspection,not guessed fields.

## Verification and discipline
One independent teammate inspects server-reference semantics; leader validates our implementation and runs GPU work (no subagent verification of leader code). Per-iteration goal/hypothesis/command/output/evidence. Preserve code HEAD+dirty diff+complete source hashes for every run. Verify packaged source and copied seeds rather than assume filenames imply equivalence. No new server launch is required or planned. If GPU access or matched-state initialization is blocked,report exact blocker. Stop timers and own container when finished;do not cancel existing holds.
