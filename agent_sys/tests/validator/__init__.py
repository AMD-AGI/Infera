"""`tests/validator` is a package for the import-mode reason `task_graph` design
§11 gives: without `__init__.py`, two test modules of the same basename in
different directories collide under pytest's default rootdir insertion."""
