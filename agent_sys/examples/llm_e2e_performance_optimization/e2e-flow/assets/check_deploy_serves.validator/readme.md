# `check_deploy_serves`

**SKELETON — the owner replaces this file.**

Mission rule G4.1: program by default. If this ends up `kind: ai`, G4.2.1 applies
and the body of this readme becomes a `STEPS` section — ordered commands, each
with its acceptance criterion — and G3.1 applies to the criteria, which go in
yaml with a name, a brief and a standard per row.

## Criteria

*(owner)*
