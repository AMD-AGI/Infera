# `m1_deploy`

A non-leaf: its work is its subgraph, and the two are mutually exclusive
(`closure` spec 2.6). Nothing runs here.

See `../../CONTRACT.md` for the frozen kind list and `../../steps/` for the wiring.
