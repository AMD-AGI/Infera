Bring this model up in the requested mode on this host, prove it answers through the router, and hand back a kit somebody else can run. Follow the task readme STEPS, including the STEP 8 self-check against assets/schemas/deploy_kit.layout.yaml. Write the kit scripts FIRST. Then perform EXACTLY ONE bring-up, using the delivered scripts, prove it serves, tear it down, and seal. Do not bring the model up more than once - that single cycle is the shipped-bytes cycle, so the evidence still comes from the delivered code, and repeating it is what has made this stage slow. Every identifier the kit binds - each port, container name and work root - must be written as : "${NAME:=default}" so a second copy of this kit can run beside the first, and this is checked. Do not introduce a second name for a value that already has one: the previous kit added DK_ROUTER_PORT beside the existing DK_PORT_ROUTER and assigned it unconditionally, and that was refused. In the kit deploy script preflight: when a card you were told to take reads busy, do not abort on the first reading. Identify the holder by CONTAINER LABEL FIRST - docker ps filtered on label deploy_kit_owner=yihou crosses container boundaries. KFD process inspection is only a supplement and must never be the deciding test: /proc cannot see into other containers, so a card held by one of our own containers shows NO KFD process at all, and treating that as unidentified is exactly what aborted the previous run. Then: if a container carrying our label holds the cards, it is a predecessor still releasing memory, so wait and re-read for up to 300 seconds. If a foreign container holds a GPU, abort immediately as a stranger. If nothing is visible at all and the cards still read busy, WAIT as well and abort only after 300 seconds, because nothing visible means the instruments are blind rather than the card being free. Substituting a different card stays out of scope in every case. The abort message must say which instruments were tried, in what order, and what each returned - for instance that no container carries our label AND no KFD process is visible AND the cards read N bytes - so the next reader can tell a blind guard from a genuine stranger. Keep writing the deploy_kit_owner=yihou label on every container the kit starts, because this check depends on it. Out of scope: bringing up any additional deployment or configuration this run does not use - the work root is local disk and NFS is not used here - and executing the graph validators yourself. If you find a defect outside this scope, record it in the kit notes and seal anyway.