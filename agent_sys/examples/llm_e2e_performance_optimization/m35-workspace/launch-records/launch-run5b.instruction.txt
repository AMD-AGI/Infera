Bring this model up in the requested mode on this host, prove it answers through the router, and hand back a kit somebody else can run. Follow the task readme STEPS, including the STEP 8 self-check against assets/schemas/deploy_kit.layout.yaml. Write the kit scripts FIRST. Then perform EXACTLY ONE bring-up, using the delivered scripts, prove it serves, tear it down, and seal. Do not bring the model up more than once - that single cycle is the shipped-bytes cycle, so the evidence still comes from the delivered code, and repeating it is what has made this stage slow. Every identifier the kit binds - each port, container name and work root - must be written as : "${NAME:=default}" so a second copy of this kit can run beside the first, and this is checked. Do not introduce a second name for a value that already has one: the previous kit added DK_ROUTER_PORT beside the existing DK_PORT_ROUTER and assigned it unconditionally, and that was refused. THE KIT DEPLOY SCRIPT PREFLIGHT - READ THIS WHOLE PARAGRAPH BEFORE WRITING IT, BECAUSE THE PREVIOUS KIT GOT IT WRONG AND KILLED A RUN. When a card you were told to take reads busy, do not abort on the first reading. Identify the holder by CONTAINER LABEL FIRST - docker ps filtered on label deploy_kit_owner=yihou crosses container boundaries. A CONTAINER COUNTS AS HOLDING A GPU ONLY IF NON-ZERO GPU MEMORY IS ATTRIBUTABLE TO IT. Mapping /dev/kfd into a container is NOT evidence that it holds a GPU: long-lived development containers on this host map /dev/kfd permanently and hold zero VRAM, so a test on docker inspect HostConfig.Devices names them on every single run, and a stranger branch keyed on that test fires every time a card reads busy for any reason whatsoever - that is exactly what aborted the previous run at its second stage. HostConfig.Devices sees the mapping and not the use, which is the same defect as KFD process inspection and must be demoted the same way. KFD process inspection is likewise only a supplement and must never be the deciding test: /proc cannot see into other containers, so a card held by one of our own containers shows NO KFD process at all. Use rocm-smi occupancy to decide, and WHEN YOU CANNOT ATTRIBUTE THE MEMORY TO A SPECIFIC CONTAINER THE HONEST CLASSIFICATION IS UNKNOWN, AND UNKNOWN WAITS - IT DOES NOT ABORT. So: if a container carrying our label holds the cards, it is a predecessor still releasing memory, so wait and re-read for up to 300 seconds. ABORT AS A STRANGER ONLY ON A POSITIVE DETERMINATION - you can name a container AND you can attribute non-zero VRAM to it - and in every other case WAIT and abort only after 300 seconds, because nothing attributable means the instruments are blind rather than the card being held by someone else. EXPECT THIS TO HAPPEN ON A HEALTHY RUN: the graph validator that precedes the next stage performs its own bring-up on these same cards and its container is removed while VRAM is still draining, so a busy card with NO container to attribute it to is normal for up to about sixty seconds after a preceding stage finishes. That is the single most likely thing your preflight will meet, and it is precisely the case the wait exists for - the wait is the expected path, not an exceptional one. Substituting a different card stays out of scope in every case. The abort message must say which instruments were tried, in what order, and what each returned, and for a stranger abort it must state which of the two conditions was met and how you measured the attributable memory - so the next reader can tell a blind guard from a genuine stranger. Also record how many seconds you waited, in the preflight result you write to disk, even when that number is zero. Keep writing the deploy_kit_owner=yihou label on every container the kit starts, because this check depends on it. Out of scope: bringing up any additional deployment or configuration this run does not use - the work root is local disk and NFS is not used here - and executing the graph validators yourself. If you find a defect outside this scope, record it in the kit notes and seal anyway. TWO MORE THINGS THE PREVIOUS KIT GOT WRONG, BOTH IN BRING-UP, BOTH MEASURED TODAY. FIRST, EVIDENCE: capture the etcd container's logs to logs/etcd.log UNCONDITIONALLY, on the success path as well as every failure path - docker logs "$DK_CONTAINER_ETCD" > "$LOGDIR/etcd.log" 2>&1 at the end of deploy and again in teardown. A previous kit wrote it only on one path, and when a router failed to reach etcd two separate people read the missing file as evidence and both were wrong in opposite directions - the absence of that log established nothing and destroyed the only thing that could have priced the failure. Do the same for any other container you start: if you start it, you capture its log. SECOND, DO NOT ADD ANY RETRY OR EARLY ABORT KEYED ON LOG TEXT DURING BRING-UP, AND IN PARTICULAR NOT ON THE DETOKENIZER HEALTH-CHECK LINES. A previous instruction told a kit to treat "The server is fired up and ready to roll!" followed by "Health check failed. Server couldn't get a response from detokenizer" as a retryable fault. IT IS NOT A FAULT. Measured across every bring-up on this host: 21 of 21 worker logs carry both lines, three to five occurrences each, INCLUDING every bring-up that went on to serve successfully. The lines are ordinary cold-start noise while weights load and JIT kernels build, and the previous kit's own console printed exactly that -- "the repeated health-check failure in the log is that, not a hang" -- thirty seconds before it aborted on them. That guard killed two healthy bring-ups at 130 seconds against a 2400 second budget and the router was never even started. The correct readiness test is the one the kit already has and it is the only one you need: poll the worker's /health endpoint until it answers, with the full timeout budget, and abort early ONLY if the worker PROCESS has actually died. Do not grep the log for trouble and act on it. If you believe some log line warrants an early abort, print it as a note and keep waiting; a wasted wait costs minutes, and a wrong early abort costs the run.
