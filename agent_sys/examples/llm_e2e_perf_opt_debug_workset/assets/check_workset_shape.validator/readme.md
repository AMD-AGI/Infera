# check_workset_shape (debug harness copy)

The real validator, run through a shim. Its rules, its args and its failure
messages are the ones documented beside the real body — this file exists only
because the assets convention requires a readme next to every entry.
