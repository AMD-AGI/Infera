# check_thing

Completeness / strong. Every row carries the keys named in args.required_row_keys
and the totals are recomputed. One validator spec shared by three kinds, matching
the `ok.algorithms_solve_grade.14` `check_compiles` pattern across
solutions_a/b/c.
