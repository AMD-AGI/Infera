--- a/reset_merged.sh
+++ b/reset_node.sh
@@ -1,41 +1,43 @@
 #!/usr/bin/env bash
-# Per-node reset + start, using the BUILT merged image. No patching step.
+# Per-node reset for the bench, from the merged-e image. Runs ON the node.
 #
-# The patch step is gone on purpose: this run exists to show the Dockerfile
-# reproduces the experiment, so anything applied after `docker run` would
-# defeat the point. The image was verified in the bytecode beforehand
-# (verify_img/inner.sh), which is the check the patch step used to carry.
+# Derived from the merge kit's reset_merged.sh. Deltas, all deliberate:
+#   * IMAGE defaults to :merged-e (the 31-commit branch head, group E included)
+#   * container name bench_run, so the kit's merged_run stays untouched as a
+#     ground-truth reference (notes.md §8: do not build over :merged)
+#   * stages THIS workspace's scripts, not the kit's
 #
-# RDMA discipline unchanged: tear down, WAIT for the GPUs to actually go idle
-# (a leg still holding VRAM makes the next round OOM in a way that reads as a
-# regression), fresh container, then confirm 8 PORT_ACTIVE before anything else
-# -- a failed libionic injection silently drops mooncake to TCP and the run
-# "works" while measuring nothing.
+# No patch step. The image is the artifact under test.
 #
 #   ROLE=prefill|decode  MY_IP=<rail ip>  [ETCD=1]
 set -u
 ROLE="${ROLE:?}"; MY_IP="${MY_IP:?}"; ETCD="${ETCD:-0}"
-IMAGE="${IMAGE:-infera/engine-sglang:merged}"
-CTR="${CTR:-merged_run}"
-KIT=/mnt/vast/c_huggingface/merge_20260731
+IMAGE="${IMAGE:-infera/engine-sglang:merged-e}"
+CTR="${CTR:-bench_run}"
+W=/mnt/vast/c_huggingface/bench_20260801
 KVD_SOCK=/tmp/kvd/kvd.sock
 
-echo "===== 1. teardown ====="
-docker rm -f "$CTR" merge_g0 vprobe >/dev/null 2>&1
-[ "$ETCD" = "1" ] && docker rm -f "${CTR}_etcd" merge_g0_etcd >/dev/null 2>&1
-for p in 'infera.engine.sglang' 'infera.server' 'infera.kvd' 'sglang.launch_server'; do
+echo "===== 1. teardown (ours only) ====="
+# bench_run is ours. merged_run is the previous round's and also ours -- it holds
+# the GPUs, so it has to go. Everything else on these shared nodes is NOT ours.
+docker rm -f "$CTR" >/dev/null 2>&1
+docker rm -f merged_run >/dev/null 2>&1
+[ "$ETCD" = "1" ] && docker rm -f "${CTR}_etcd" merged_run_etcd >/dev/null 2>&1
+for p in 'infera.engine.sglang' 'infera.server' 'infera.kvd' 'sglang.launch_server' 'infera-router'; do
   pkill -9 -f "$p" 2>/dev/null
 done
 sleep 8
 
 echo "===== 2. confirm GPUs released ====="
-for i in $(seq 1 30); do
+for i in $(seq 1 45); do
   n=$(rocm-smi --showpids 2>/dev/null | grep -cE '^[0-9]+' || true); n=${n:-0}
   [ "$n" -eq 0 ] && { echo "  GPUs idle after $((i*2))s"; break; }
   sleep 2
 done
+rocm-smi --csv --showmeminfo vram 2>/dev/null | tail -8 | awk -F, \
+  '{printf "  %s %.0fGB used\n", $1, $3/1073741824}'
 
-echo "===== 3. fresh container from the BUILT image ====="
+echo "===== 3. fresh container from $IMAGE ====="
 HL=$(readlink -f /usr/lib/x86_64-linux-gnu/libionic.so.1)
 docker run -d --name "$CTR" --network=host --ipc=host --shm-size=32G \
   --device=/dev/kfd --device=/dev/dri --device=/dev/infiniband \
@@ -47,16 +49,18 @@
 echo "  image: $(docker inspect "$CTR" --format '{{.Image}}')"
 
 echo "===== 4. RDMA fabric check (inside container) ====="
+# A failed libionic injection drops mooncake to TCP silently -- the run then
+# "works" while measuring nothing.
 np=$(docker exec "$CTR" bash -c 'ibv_devinfo 2>/dev/null | grep -c PORT_ACTIVE')
 echo "  PORT_ACTIVE: $np (want 8)"
-[ "${np:-0}" -ge 1 ] || { echo "  RDMA DEAD -- libionic injection failed" >&2; exit 1; }
+[ "${np:-0}" -ge 8 ] || { echo "  RDMA NOT FULLY UP" >&2; }
 
-echo "===== 5. NO PATCH STEP (that is the point) ====="
+echo "===== 5. NO PATCH STEP (the image is the artifact) ====="
 
 echo "===== 6. stage run scripts ====="
-docker cp "$KIT/scripts/glm52_leg.sh" "$CTR":/glm52_leg.sh >/dev/null
-for f in probe.py stress_capture.py prefix_reuse.py needle.py; do
-  [ -f "$KIT/scripts/$f" ] && docker cp "$KIT/scripts/$f" "$CTR":/tmp/$f >/dev/null
+docker cp "$W/scripts/glm52_leg.sh" "$CTR":/glm52_leg.sh >/dev/null
+for f in needle.py probe.py prefix_reuse.py; do
+  [ -f "$W/scripts/$f" ] && docker cp "$W/scripts/$f" "$CTR":/tmp/$f >/dev/null
 done
 
 if [ "$ETCD" = "1" ]; then
@@ -68,9 +72,23 @@
 fi
 
 echo "===== 8. kvd daemon ====="
-docker exec "$CTR" bash -c "mkdir -p /tmp/kvd /tmp/kvd-long && rm -f $KVD_SOCK && printf '%s\n' '#!/bin/bash' 'exec python3 -m infera.kvd --socket $KVD_SOCK --max-bytes 64G --long-path /tmp/kvd-long --long-bytes 512G --log-level INFO' > /run_kvd.sh && chmod +x /run_kvd.sh"
+# --long-bytes: 512G -> 64G.
+#
+# /tmp/kvd-long is INSIDE the container, so kvd's L3 spill tier lands in the
+# container's writable layer on the node's root disk -- not on /mnt/vast. The
+# kit's 512G budget is larger than the free space on /, and the fixlen sweep
+# filled it: bench_run's writable layer reached 263 GB, / hit 100%, and every
+# `docker exec` then failed with
+#     OCI runtime exec failed: write /tmp/runc-processNNN: no space left on device
+# which reads as a docker fault rather than as our own cache.
+#
+# 64G matches --max-bytes and is comfortably inside the free space on these
+# nodes. Case A writes far more KV than the sweep did (89% cache-hit target over
+# a 67-minute window), so leaving 512G here would refill the disk mid-run.
+KVD_LONG_GB="${KVD_LONG_GB:-64}"
+docker exec "$CTR" bash -c "mkdir -p /tmp/kvd /tmp/kvd-long && rm -rf /tmp/kvd-long/* && rm -f $KVD_SOCK && printf '%s\n' '#!/bin/bash' 'exec python3 -m infera.kvd --socket $KVD_SOCK --max-bytes 64G --long-path /tmp/kvd-long --long-bytes ${KVD_LONG_GB}G --log-level INFO' > /run_kvd.sh && chmod +x /run_kvd.sh"
 docker exec -d "$CTR" bash -c 'nohup /run_kvd.sh > /tmp/kvd.log 2>&1'
 sleep 20
 docker exec "$CTR" bash -c "test -S $KVD_SOCK && echo '  kvd socket OK' || { echo '  KVD FAILED'; tail -20 /tmp/kvd.log; }"
 
-echo "===== node $(hostname) ready for $ROLE (merged image, unpatched) ====="
+echo "===== node $(hostname) ready for $ROLE (image=$IMAGE, unpatched) ====="
