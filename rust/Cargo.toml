[workspace]
resolver = "2"
members = ["router"]

# Shared release profile for all Infera Rust crates. Optimised for a
# latency-sensitive network service (thin LTO keeps build times sane).
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "unwind"
