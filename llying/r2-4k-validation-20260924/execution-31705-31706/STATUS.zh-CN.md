# R2-on正式实验启动准备

用户已明确批准按已检查的R2-on/C80/P-D均4K配置启动，历史A0作为参考。配置批准及release哈希已绑定，未重做基线。

目前尚未启动模型或发压。n10-29空闲且已有基线镜像；n01-21虽然分配给当前用户，但K3服务占用8张GPU约86%利用率、274–278GiB/卡。k3.play配置用户为jihhe；主要GPU进程在root身份的k3-extract中。它们不是本任务创建的，因此等待用户确认可否停止，或等待原用户/管理员清理。

release Router已在基线镜像内编译，哈希见manifest。两个allocation独立核验；清理监控改为无GPU设备的Docker进程，避免依赖SSH子进程存活，已在P节点验证持续心跳。其预检中的Slurm账号解析问题已在模型启动前修正并留存日志。

正式结束后先保存必要的结束身份信息，立即停止Prefill启动HiCache释放，再进行离线分析。失败路径也会清理本任务捕获的容器。所有清理限定到本任务prefix、角色和镜像，不处理K3容器。
