# C1G1：在固定 C1 容量下测路由优化增量

2026-09-24。C1审计通过，但output throughput基本持平且TTFT/排队变差，不继续扩大容量、不推广扩容默认值。当前节点和P/D服务健康，直接复用C1作为本轮参考，保持P GPU 3,400,000、host 4,715,200、D GPU 3,003,264 tokens/rank，只将guard=decode改为completion。这样检验当前容量下的路由增量作用，不需要重跑基线或重启模型。

角色仍为P n02-29 / D n02-33，job31688；P/D mem fraction 0.90/0.85，P hicache ratio 1.3868141176470588，实际P/D chunk 4K/4K，C80、884 warmup、3600秒正式窗口；其余配置与C1一致。清空两端逻辑缓存，重建router/collector，记录服务PID和诊断字节游标。无法据此单独证明扩容+路由优于原容量+路由；顺序/热身影响仍存在。

首次run `c1g1-guard-completion-31688` 已通过服务验收和smoke，但在客户端启动前因UV_CONSTRAINT比较顺序错误失败，没有warmup/profiling流量。新reference C1保存的是已注入依赖锁的runtime.env，而旧wrapper先比较再注入。已修正：reference已锁定时先验证锁路径及保存的SHA256，再注入相同锁后比较；未锁定旧reference继续先比较后注入。使用真实runtime validator验证旧reference、新reference均可通过，锁内容变化与workload变化均被拒绝。失败证据保留在failed-preparation/，没有覆盖。

仅停止经/proc环境核对RUN身份的4个失败准备阶段监控进程；P/D保持运行。有效重试run为 `c1g1r-guard-completion-31688`，04:12:13 UTC启动reuse driver，PID2310087，日志为共享campaign根目录的 `c1g1r-guard-completion-31688-driver.log`。该时间只表示准备启动，尚不表示正式发压。

## 用户提出的热身混杂限制

04:14 UTC后用户明确追问不重启造成的热身偏差。当前C1→C1G1只能作为筛查，不能单独确认路由净收益。flush逻辑KV及相同884条warmup不能重置JIT、算子执行历史、分配器、页缓存或GPU温度/时钟。相同节点/进程减少节点差异，不消除顺序偏差。机制指标同步改善也不等于排除热身；不通过warmup百分比相减估计净收益。严格确认需可比启动历史及重复/交错设计；仅fresh优化配置对比旧基线同样不充分。当前不据此推广默认配置。

## 后续执行要求：正式验证重启 P/D

用户再次指出其提醒未落实到执行方式。此次C1G1确实没有重启P/D，不能把降级为筛查视为满足了用户的可信度要求。保留本轮数据并明确标注“未重启服务、存在热身和顺序混杂的探索性结果”；不得作为已确认的路由收益或默认配置变更依据。

后续正式验证重启P/D模型服务，统一启动配置及预热流程，不再默认复用。仍需记录重启后保留的JIT/系统缓存、驱动和节点条件；不声称重启消除全部混杂。旧的优先复用决策已被此要求替代。

本轮正式收尾05:36:25完成；job31688于05:42:09被标记抢占，extern于05:42:10退出，自动分析在生成summary/joined-requests后被中断。节点SSH无法访问；不能确认容器和显存已经清理。后续仅恢复离线分析，不启动新的复用实验。
