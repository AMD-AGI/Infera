# STEP 1 RESULT — GLM-5.2-MXFP4 two-node PD + DP-attention, kvaware=ON kvd=ON
#
# Ran 2026-07-30 ~11:20-11:35. prefill=chi2879 (10.2.122.10), decode=chi2867 (10.2.122.44).
# Router: infera.server :8100, --router-policy kv-aware.
# Transport: mooncake REAL RDMA (MC_FORCE_TCP=0, 8 ionic rails).
# Criterion: >=3/4.

===================== CORRECTNESS =====================

[OK] 'The capital of France is' -> '1.  Identify the core entity and question: The user is asking for the capital of France.\n2.  Retrieve knowledge: Access geographical knowledge regarding France.\n3.  Fact-check: The capital of France is Paris.\n4.  Formulate the response: State the answer clearly and concisely. "Paris'
[OK] 'The capital of China is' -> '1.  **Identify the core entity and question:** The user is asking for the capital of China.\n2.  **Retrieve knowledge:** Access geographical knowledge regarding China. The capital of China is Beijing.\n3.  **Formulate the answer:** State clearly that the capital is Beijing.\n4.  **Review'
[OK] '2+2=' -> '1.  **Analyze the Input:** The user is asking a basic arithmetic question: "2+2=".\n2.  **Determine the intent:** The user wants the sum of 2 and 2.\n3.  **Perform the calculation:** 2 + 2 = 4.\n4.'
[OK] 'The largest planet in our solar system is' -> '1.  **Identify the core entity and question:** The user is asking for the largest planet in our solar system.\n2.  **Retrieve knowledge:**\n    *   Solar system planets: Mercury, Venus, Earth, Mars, Jupiter, Saturn, Uranus, Neptune.\n    *   Sizes (by radius'

4/4 correct          <-- PASS, matches the kvaware/kvd-OFF baseline exactly

===================== CONFIG WAS REAL =====================

1. Real RDMA, not the TCP fallback:
     MC_FORCE_TCP hits in prefill log ......... 0
     "HIP dmabuf disabled" (one per NIC) ...... 8

2. PD + DP-attention, symmetric on both legs:
     prefill: disaggregation_mode='prefill' dp_size=8 enable_dp_attention=True ep_size=8
     decode : disaggregation_mode='decode'  dp_size=8 enable_dp_attention=True ep_size=8

3. kvaware ON — both workers advertise a kv-events endpoint (baseline showed null):
     prefill http://10.2.122.10:30000  kv_events_endpoint=tcp://10.2.122.10:25075  kv_block_size=64
     decode  http://10.2.122.44:30000  kv_events_endpoint=tcp://10.2.122.44:1649   kv_block_size=64
   Distinct ports on distinct nodes; the port-collision fix (patch 0001) held —
   sglang got "endpoint": "tcp://*:25075", i.e. a randomised base, not 32764.

4. infera's OWN KV probe plane DID come up (this REFUTES the earlier suspicion
   that it never attaches across the subprocess boundary):
     INFO:__main__:KV plane up: events_bind=tcp://0.0.0.0:5557
       events_advertise=tcp://10.2.122.10:5557 snapshot=http://10.2.122.10:8801
       engine_block_size=64 index_block_size=64

5. kvd ON and wired on BOTH legs, all 8 DP ranks each:
     "infera-kvd adapter connected"  prefill=8  decode=8
     Tree cache initialized: impl=HiRadixCache hierarchical=True   (both legs)
     disable_radix_cache=False                                      (both legs)
     prefill: 8x "Allocating 16.00 GB host memory for hierarchical KV cache"
   The decode leg is only legal here because infera auto-appended
   --disaggregation-decode-enable-radix-cache (confirmed present in its argv),
   which it only does when kv-events are enabled.

===================== WHAT STEP 1 DID *NOT* SHOW =====================

kvd was connected and provisioned, but served ZERO traffic:

    StatsResponse(entries=0, host_bytes=0, spillover_bytes=0, long_bytes=0,
                  gets_total=0, sets_total=0, hits_total=0, misses_total=0,
                  evictions_total=0)

Expected: the 4 probe prompts are a few dozen tokens each, share no prefix, and
never pressure the GPU cache — nothing to offload, nothing to prefetch. Note
also the startup warning that sglang 0.5.15.post1 renamed the prefetch-threshold
field, so infera's lowering of it to 64 silently no-ops; the default 256 means
short prompts never trigger an L3 prefetch either way.

Likewise the kv-aware overlap scoring had nothing to score: 4 unrelated prompts,
no shared prefix, and both role weights left at the default 1.0.

=> Step 2 (prefix-reuse workload + role weights 20.0/2.0 + a real NVMe L3 path)
   is what turns "wired and correct" into "demonstrably serving hits".
