# Run logs are already covered by the repo-root *.log rule. This one file is a
# 148 KB per-request bench dump, an outlier next to the ~2 KB summary JSONs kept
# beside it; the numbers that matter are tabulated in REPORT.zh.md §1.3.
repro_dsa/mtp1_c64.json
