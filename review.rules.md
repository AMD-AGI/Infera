# Code Review 规则（草稿）

本文件由一次真实的 review 过程（PR #58，sglang DSA patch set）总结而成。每条
规则后面都附了它对应的**实际事故**——规则不是从原则推导出来的，是踩出来的。

状态：草稿。尚未在团队内达成一致，也尚未挂到 CONTRIBUTING.md 上。

---

## 一、rebase 与合并

### 1.1 「rebase 无冲突」不等于「没有重合」

git 只在**同一文件的同一区域**被双方修改时才报冲突。如果分支只是**新增文件**，
而 main 用**另一个文件**解决了同一个问题，git 会安静地把两份都留下。

> 事故：分支带了 `deepseek_nextn_glm52_mtp_bf16.diff`，main 用
> `patch_glm52_nextn_quark_exclude.py` 做了**同一处**修改。rebase 零冲突。但
> main 的脚本在 Dockerfile 里先跑，我们的 context diff 随后在
> `--fuzz=0` 下撞上已被改过的锚点——必然的构建失败，而不只是冗余。

**要求**：rebase 之后，不要只看 `git status`。对分支触碰的每个主题，在 main 上
搜一遍等价实现（关键词、符号名、issue 号），逐项判断是重合、互补还是无关。

### 1.2 中间 commit 不可信时，压成一个

只有分支的**最终状态**被验证过。中间 commit 往往包含后来被证伪的实现或被纠正
的描述。若按 merge commit 合入，这些错误状态会永久留在 main 的历史里，
`git blame` 会指向一个已知错误的版本。

> 事故：本 PR 首个 commit 里的 patch 1 带着 `0 < q_offset` 下界，会让 DP-idle
> rank 触发 assert；另一个 commit 对 patch 2a 证据的描述后来被专门的 commit
> 纠正。这两个状态都不该进入 main 的历史。

**要求**：合入前确认仓库的合并方式（`gh api repos/OWNER/REPO -q '.allow_*'`）
以及历史惯例（`git log --oneline` 看有没有 `Merge pull request`）。若历史会被
原样保留，而中间 commit 未经验证，则先自行 squash，不要依赖合并者去选。

**压平后必须验证只改了历史、没改内容**：

```bash
git rev-parse HEAD^{tree}      # squash 前后必须完全一致
git diff <pre-squash-sha> HEAD # 必须为空
```

---

## 二、注释与文档

### 2.1 除文件级注释外，注释不超过 3 行

按 **what / why / how / before / after / context** 组织。写不下的，说明它属于
patch 头部或设计文档，不属于代码行间。

### 2.2 详情移走，不是删掉

压缩注释时，被移出的论证必须落到一个**明确的去处**（patch 头部、README、状态
表），并在原地留下指路的一句话。否则下一个人会把已经排除过的方案再试一遍。

### 2.3 记录已被证伪的路径

「不要这样写」和「要这样写」同等重要，尤其当错误写法**能通过全部功能测试**时。

> 事故：patch 4 的谓词若误用 `future_indices is not None`，draft graph 会被
> 100% 拒绝（实测 0.0%，200/200），把修复悄悄降级成「关掉该特性」的
> workaround——而所有功能测试照样全绿。这一条必须写在注释里。

---

## 三、Patch 的上游归属

### 3.1 每个 patch 必须标注四项

1. 对应的 upstream **issue**
2. 对应的 upstream **他人 PR**
3. 对应的 upstream **本人 PR**
4. 本 patch 与**本人 PR** 的差异：base、实现形式、本地化/去本地化的内容、
   取舍（各自更强在哪）、各自的验证依据

### 3.2 「没有上游」要说明是怎么查的

「未找到」和「不存在」不是一回事。`gh search` 匹配的是标题和正文，**不匹配 diff
内容**，所以上游完全可能改了同一处而没提到它。写明检索方式与其局限。

### 3.3 上游状态必须现查，不得凭记忆

**要求**：用 `gh` 查，写明查询日期，并注明状态会漂移。

> 事故：本仓库此前有三处对上游状态的断言是凭笔记写的，事后全部被证伪——包括
> 「这个机制似乎无人报告」（实际上游 issue 早两天就报了同一问题）。

### 3.4 已合并 ≠ 可以删除本地 patch

判据是「**已合并且已进入 pinned base**」。已合并但 base 未跟进的，本地 patch
仍需保留。状态表里要把这两种情况区分开。

### 3.5 建立全仓库的上游索引表

一张表覆盖所有 patch：路径、一句话问题、upstream issue、upstream PR、
**该 PR 是否本仓库作者发起**、PR 状态。表是索引，不是记录；完整论证放在各
patch 自己的头部。

---

## 四、验证

### 4.1 证明，而不是自述

改完之后声称「我只改了注释」是**没有证据的**。要用机械手段证明。

> 本次采用：把改前/改后的 patch 分别应用到 pinned base，比对 **AST dump**
> （AST 天然忽略注释，docstring 文本另做归一化）。8/8 文件完全相同——「只改了
> 注释」于是成为构造性结论，而不是我读一遍下的判断。

### 4.2 拿已验证产物做基准（对拍）

**改动后的产物必须与最后一次真实验证过的产物做等价比对**，而不是只跟自己的上
一版比。

### 4.3 `--dry-run` 默认带 fuzz，不算验证

`patch --dry-run` 和 `git apply --check` **默认允许 fuzz**。真正的验证是拉一份
pinned commit 的干净源码树，实际应用，再字节编译。

> 事故：本系列曾有一个手写 diff 在「检查通过」的情况下静默丢了一个 hunk。

### 4.4 端到端跑真实脚本，不要只跑其中的片段

改了任何被构建脚本消费的东西（包括**注释文本**，如果脚本 grep 它），必须完整跑
一遍那个脚本。

> 事故：patch 2a 没有引入新标识符，构建脚本只能 grep 它的注释原文来确认落地。
> 本次改写了那句注释，脚本的 grep 串必须同步——否则构建在别人机器上才炸。

### 4.5 shell 扫描要先自证有效

一个「零命中」的扫描，可能是真干净，也可能是命令根本没生效。

> 事故：本次泄露审计前三轮报告「全部 (none)」，实际上 zsh 下 `$FILES`
> 未做词分割，grep 根本没收到文件列表——同一批扫描把 `llying`、
> `Infera-Private` 一并漏报了。

**要求**：用数组 `"${F[@]}"` 而非裸变量；先用一个**已知必然命中**的模式验证
扫描本身有效，再相信它的「零命中」。

---

## 五、进入正式仓库前的信息清理

只审查**本次 PR 的新增内容**（`git diff origin/main HEAD`），不追究历史遗留。

### 5.1 必查清单

| 类别 | 示例 |
|---|---|
| IP / 主机名 | 集群节点名、`*.compute.internal` |
| 本地绝对路径 | `/home/<user>/...`、共享存储挂载点 |
| 调度器 / 集群标识 | slurm 命令、分区名、预约名、集群代号 |
| 个人分支 / 实验目录名 | 对外部读者是死链，且常含集群代号 |
| 他人姓名 / 账号 | 尤其是挂在**未经我方验证**的数据上 |
| 私有仓库链接 | 暴露其存在与 issue 编号 |
| 凭据 | token、密钥、密码 |

### 5.2 以仓库既有惯例为准，不要自作主张地更精确

> 事故：仓库既有写法是 `AMD-AGI/Infera#67`。我在新表里「订正」成了带可点击
> 链接的 `AMD-AGI/Infera-Private#67`——比原文更准确，但把私有仓库的存在和编号
> 一起暴露了。**更精确未必更合适。**

### 5.3 删除可追溯性时要留下替代路径

不要直接删掉证据指针。改成「内部复现套件，含 X/Y/Z 各一份，向作者索取」——
读者仍然知道**证据存在、覆盖了哪些面**，只是拿不到路径。

### 5.4 保留证据强度标注

去掉人名，但保留 second-hand / 未实测 / 单次观察 之类的限定语——那才是判断该条
论断可信度的依据。

### 5.5 产品名不必清理

`MI355X`、`Blackwell` 是公开产品名，且已在仓库中广泛使用。清理要有边界，否则
文档会被削得没有信息量。

---

## 六、DCO

### 6.1 校验的是 author email，不是「有没有 trailer」

`git rebase --signoff` 只按本地 config 追加 trailer，**从不改写 authorship**。
所以对一个 author 邮箱不对的 commit，CI 提示的那条命令**永远修不好它**。

> 事故：本 PR 首个 commit 的 author 是集群主机名
> `yihou@crs-m2m-cpu-spur-012...`（在集群上未设 git config 时提交）。补了
> sign-off 之后 DCO 依然失败。

**推送前自查**：

```bash
git log --format='%h A=%ae SOB=%(trailers:key=Signed-off-by,valueonly)' origin/main..HEAD
```

author 有问题时改写它，再把后续 commit 接上；接完必须确认 `git diff` 为空，
证明只改了 authorship：

```bash
git checkout <bad-sha> && git commit --amend --no-edit --author="Name <you@example.com>"
git rebase --onto <new-sha> <bad-sha> <branch>
```

**预防**：在每个新的容器 / checkout 里先设 `git config user.email`。

---

## 七、Review 的沟通方式

### 7.1 有决策价值的分歧，先汇总再执行

不要边发现边改。把发现分档（严重度 / 是否本次新引入 / 是否有仓库先例），
给出建议，交由决策——尤其当「修」和「不修」都站得住时。

### 7.2 区分「本次新引入」和「早已存在」

`MI355X` 在 main 上有 66 处，`packup_` 是 0 处。前者是既有惯例，后者是本次新
引入——两者的处置门槛完全不同。给结论时必须带上这个区分。

### 7.3 证据不足就挂起，不要给判断

发现的问题若无法立即定性（例如上游那个未解的 conc=32 失败），如实写「十七个
候选原因已排除，根因未确定」，不要为了收尾给一个猜测。

### 7.4 自己的错误要更正，且要说清影响面

> 本次：前三轮泄露扫描的「干净」结论是无效的（见 4.5）。重扫后结论恰好也是
> 干净，但当时那个判断没有依据，而且同一批扫描漏报了两类真实命中。这一点必须
> 主动讲明，不能因为「结果一样」就略过。
